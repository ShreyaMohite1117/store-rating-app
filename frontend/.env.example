# Base URL of the backend API.
# When running both apps locally with the default Vite proxy (see vite.config.js),
# you can leave this as /api and requests will be forwarded to http://localhost:5000.
VITE_API_BASE_URL=/api
